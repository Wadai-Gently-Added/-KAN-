# PROJECT_STRUCTURE.md — 換-KAN-

最終更新：2026-09-13（v1.0.8）

## v1.0.8での変更点

- **改善：** フォルダ選択ダイアログを毎回「ダウンロード」フォルダから開始するように変更
  （`showDirectoryPicker`に`startIn: "downloads"`を指定。前回選んだ場所の奥深くから再度潜る手間を軽減）
- **修正：** 同じ変換元（例：`.md`）のルールを複数「有効」にすると、対象拡張子が丸ごと除外されて
  プレビューが無言で空になる問題を改善。
  - 重複判定は「有効なルール同士」でのみ行うように変更（片方を無効にすれば自動的に解消）
  - 重複しているルールの行を赤くハイライトし、チェックボックスの切り替えに応じて即座に表示を更新
  - プレビューが0件の時、「フォルダ未登録」「ルール未登録」「ルール重複」を区別したメッセージを表示
- **既知の制約として明記：** File System Access APIの仕様上、フォルダの複数選択は不可能
  （ダイアログ上で複数ハイライトしても、ページに渡ってくるのは常に1個）

## v1.0.7での変更点

- **改善：** フォルダを新規追加した時のデフォルトを「サブフォルダを含める＝ON／階層＝無制限」に変更
  （従来は「0階層のみ」がデフォルトで、多くの人が最初に無制限へ変更する手間があったため）

## v1.0.6での変更点

- **追加：** 変換ルールをブラウザの`localStorage`に保存し、次回起動時も自動で復元されるように
- **追加：** 各ルールに「有効/無効」チェックボックスを追加。登録だけしておいて今回使わないルールは
  チェックを外すだけで一時的に無視できる（削除しなくてよい）
- **追加：** ルールに反転ボタン（⇄）を追加。変換元と変換先を1クリックで入れ替えられる
- **改善：** フォルダ一覧の各行を、プレビューのグループ見出しと同じ色帯で表示するように変更
  （小さい丸アイコンだけでなく、行全体の背景・左端の線が同じ色相になるよう統一）
- **改善：** 画面幅が狭い時に、フォルダ／ルールの各行が縦積みレイアウトに切り替わるようにして、
  文字や入力欄が窮屈にならないよう調整

## v1.0.5での変更点

- **改善：** 「衝突あり」→「既存あり」に表記変更（ログ・サマリー・Undo/Redoメッセージも統一）
- **改善：** プレビューをフォルダごとにグループ表示に変更
  - フォルダ登録順にグループ見出し行を挟んで表示（「フォルダ」列は廃止）
  - フォルダごとに固定の色相を割り当て、フォルダ一覧の名前の横にも同じ色のスウォッチを表示
  - 同じフォルダ内でも、サブフォルダの階層が深いファイルほど明るい色合いになるようにして、
    同系色でまとまりつつ階層が視覚的に分かるようにした

## v1.0.4での変更点

- **修正：** 更新をGitHubに上げてもブラウザに反映されない問題を修正。
  原因は`sw.js`（Service Worker）の「キャッシュ最優先」戦略。`sw.js`自体の中身がバージョン間で
  変わっていなかったため、ブラウザがService Workerの更新を検知できず、最初にキャッシュした
  古いバージョンをずっと返し続けていた。
  → **「ネットワーク優先、失敗時のみキャッシュ」**方式に変更し、`CACHE_NAME`もバージョンごとに更新するようにした。
  既にブラウザに残っている古いService Workerは手動リセットが必要（README.md参照）。

## v1.0.3での変更点

- **修正：** 複数フォルダを一度に実行すると2件目以降が
  `User activation is required to request permissions` で失敗するバグを修正。
  - フォルダ追加時に `showDirectoryPicker({ mode: "readwrite" })` で最初から読み書き権限を取得
  - 実行/Undo/Redoの直前に、関係する全フォルダの権限をまとめて確認する処理を追加
    （ユーザー操作の有効時間が複数フォルダの処理をまたぐと切れてしまうため、最初に一括で確認する方式に変更）

## v1.0.2での変更点

- **改善：** フォルダの参照が無効になった時（例：Zip再展開でフォルダが削除・再作成された）に、
  分かりやすい日本語メッセージ＋フォルダ一覧への「⚠ 再登録が必要」バッジ表示を追加
  （エラー名 `NotFoundError` を判定して分岐）

## v1.0.1での変更点

- **修正：** フォルダを削除してもプレビュー表に古い行が残るバグを修正
  （`removeFolder`が`state.preview`側を更新していなかったため）
- **追加：** プレビュー各行にチェックボックスを追加し、実行対象を個別選択できるように。
  ヘッダーに全選択トグルつき（衝突/実行済み/失敗の行はチェック不可）

```
kan-tool/
├─ index.html          … 画面構造（フォルダ／ルール／プレビュー／実行）
├─ styles.css           … 見た目（墨×藍のダークトーン、モノスペースでパス表示）
├─ app.js                … コアロジック
│   ├─ state              … folders / rules / preview / history / redoStack
│   ├─ フォルダ管理        … addFolder / removeFolder / renderFolders
│   ├─ ルール管理          … addRule / removeRule / renderRules / ruleValidationMessage
│   ├─ 探索                … scanDirectory（再帰・深さ制限つき）
│   ├─ プレビュー          … buildPreview / renderPreview（行ごとの selected フラグ、全選択トグル）
│   ├─ 実行                … renameOne（作成→書込→検証→削除）/ executeAll
│   └─ Undo/Redo           … undoBatch / undoLast / redoLast
├─ manifest.json         … PWAマニフェスト（アイコン未設定＝要追加）
├─ sw.js                  … Service Worker（オフラインキャッシュ、静的アセットのみ）
├─ README.md              … 使い方・既知の制約
└─ PROJECT_STRUCTURE.md   … このファイル
```

## 状態管理の方針

- フォルダとプレビュー・履歴は**インメモリ**（ページを閉じると消える）。IndexedDBへのフォルダハンドル永続化はv1では未実装。
- **変換ルールのみ**`localStorage`（キー: `kan-tool-rules-v1`）に保存し、次回起動時も復元される。
- フォルダの`handle`、ファイルの`fileHandle`/`dirHandle`はJSオブジェクトとして直接保持し、
  ページリロードをまたいでは使えない前提。

## 次にやるなら（優先順）

1. Redoの安定性向上・エラー処理強化（仕様書の優先順位どおり）
2. フォルダハンドルのIndexedDB永続化＋権限再要求フロー
3. アイコン画像の用意（`manifest.json`の`icons`が空のまま）
4. 使用感を見た上で iOS対応 or デスクトップアプリ化を再検討

## 既知の制約（設計上の判断）

- リネームは「作成→書込→検証→削除」の疑似操作（3. 仕様確認の回答を参照）
- Safari/iOS/Firefoxは非対応（起動時に自動検出してバナー表示）
- Undo/Redoはセッション内限定
